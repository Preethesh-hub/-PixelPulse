// JavaScript functionality will be implemented in Part 2.
// Currently listening for search form submission to prevent default reload.

document.getElementById('search-form').addEventListener('submit', (e) => {
    e.preventDefault();
    console.log('Search triggered, but no behaviour implemented yet!');
});
